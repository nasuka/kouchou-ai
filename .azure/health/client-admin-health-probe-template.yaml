properties:
  template:
    containers:
      - name: client-admin
        image: ${CLIENT_ADMIN_IMAGE}
        probes:
          - type: liveness
            httpGet:
              path: /api/healthcheck
              port: 4000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1
    scale:
      minReplicas: 1
      maxReplicas: 1